# ============================================================
# Programmers Lab — Apache Security Rules
# ============================================================

# Custom 404 error page
ErrorDocument 404 /pl/page-404.html

# Prevent directory listing
Options -Indexes

# Block direct access to sensitive PHP config files
<FilesMatch "(config\.php|\.env|\.gitkeep|test_forms\.php|create_admin\.php)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Block SQL files from direct browser access
<FilesMatch "\.sql$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Block readme/documentation files
<FilesMatch "(README\.md|BACKEND_README\.md)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Block access to logs directory
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^logs/ - [F,L]
</IfModule>

# Block ALL script execution inside uploads folder (security critical)
<IfModule mod_rewrite.c>
    RewriteRule ^uploads/.*\.(php|php5|php7|phtml|phar|shtml|cgi|pl|py|sh|bash|jsp|asp|aspx)$ - [F,NC,L]
</IfModule>

# Block access to .git folder
<IfModule mod_rewrite.c>
    RewriteRule ^\.git/ - [F,L]
</IfModule>

# Force HTTPS (uncomment on live server with SSL)
<IfModule mod_rewrite.c>
    RewriteCond %{HTTPS} off
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>

# Prevent hotlinking of images
<IfModule mod_rewrite.c>
    RewriteCond %{HTTP_REFERER} !^$
    RewriteCond %{HTTP_REFERER} !^https?://(www\.)?programmerslabs\.com [NC]
    RewriteCond %{HTTP_REFERER} !^https?://lavender-giraffe-648250\.hostingersite\.com [NC]
    RewriteCond %{HTTP_REFERER} !^https?://localhost [NC]
    RewriteRule \.(jpg|jpeg|png|gif|webp)$ - [F,NC,L]
</IfModule>

# Security Headers
<IfModule mod_headers.c>
    Header always set X-Frame-Options "DENY"
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    # HSTS — tells browsers to always use HTTPS for 1 year (live server only)
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
</IfModule>

# Disable server signature
ServerSignature Off
